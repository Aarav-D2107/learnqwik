# LearnQwik backend — pinned for reproducible Render builds.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
email-validator==2.2.0
python-multipart==0.0.20
httpx==0.28.1

# Document intelligence
PyMuPDF==1.25.1          # PDF text extraction + page rasterization for Vision
python-pptx==1.0.2       # PPTX slides, tables, embedded images
python-docx==1.1.2       # DOCX paragraphs, tables, embedded images
Pillow==11.0.0           # image handling

# Tests
pytest==8.3.4
pytest-asyncio==0.25.0
